# Implementation Plan

This file is reserved for stable implementation plans after architecture review.

## Current Rule

Do not edit app code from this file alone. Each patch still needs an explicit implementation target in the main thread or a clear board assignment.

Treat Andrews lesson rows as curriculum/status indexes. The engine and UI should be organized around reusable grammar categories, slot metadata, diagnostics, and controls; do not create one engine, route, or control merely because a lesson exists.

Andrews is the grammar-rule authority for slot order, categories, and dependencies, but not for Nawat/Pipil spelling. Use `convertClassicalLettersToNawat()` / `getClassicalLettersAsNawat()` when an Andrews grammar-rule spelling must become a Nawat/Pipil-letter engine surface, e.g. `z/liz -> s/lis`, `lo-hua -> lu-wa`, `hua-lo -> wa-lu`, `c-ti-ya -> k-ti-ya`, and `(xochi)-tl -> (shuchi)-t`. The conversion is orthography realization, not lexical fixture evidence.

## Active Target

Lessons 54-55 denominal route-family metadata checkpoint.

Status: current denominal route support remains route-based and partial. Andrews §54.2/§54.4 support the current `vi-ti` route suffix contract, and Andrews §55.6 supports the current `vi-iwi`/`vi-awi` route suffix contracts after Classical `i-hui`/`a-hui` pass through `convertClassicalLettersToNawat()` as `i-wi`/`a-wi`. The existing `vt-na` route remains current Nawat route data only: Andrews Lesson 55 has transitive denominal `i-a`, `o-a`, and `huia` contracts, but no `-na` denominal suffix contract in the verified PDF wording, so `vt-na` is marked `nawat-transitive-route-no-andrews-suffix` and `noAndrewsSuffixContract`. `getNawatDenominalAndrewsContractInventory()` now exposes the verified §54.2-§55.7 NNC-to-VNC denominal contract inventory as structural metadata, including §54.2 `ti`, `hui`, `hui-lia`, `ya`, deverbal `ti-ya`/`hui-ya`, `ya-lia`, limited `a`, and deverbal `hua`, plus included-possessor `ti`, temporal `tia`, causative `tla`, the §55.2 `tla -> ti-lia` applicative, less-productive intransitive `tla`, the §55.2 note's intransitive `tla -> ti-a/ti-lia` continuations, §55.3 `o-a`/`huia`, the §55.3 note 2 `o-a` source to hypothetical `i-l-huia`/`a-l-huia` applicative path, §55.6 `i-hui`/`a-hui > o-a`, and transitive `i-a` contracts with Classical spellings converted to Nawat/Pipil rule letters. Existing source-state/generated-output metadata derives `denominalFamilyProfile` from configured route data, `getNawatDenominalRouteFamilyInventory()` exposes the same configured families as partial route-based coverage, and `generateNawatDenominalRouteFamilyPreview()` links configured source/stem/verbalizer/target/surface trail stages for a source input. Preview stages carry `nextSource` candidates with input/mode/tense context for later composition; selected `vi-ti` verbalizer stages carry bounded generated `ti` source evidence for the §54.2 `ti-ya`, §54.2/§54.4 `ti-lia`, §54.5 `ti-a`, and §54.6 `t-ia` continuations, selected Andrews `hui` targets carry bounded source evidence for `hui-ya` and `hui-lia`, and selected `vi-iwi`/`vi-awi` verbalizer stages carry bounded `i-hui`/`a-hui` source evidence into the next route preview so Andrews §55.6 can unlock the matching `o-a` counterpart from the generated source stage rather than from a bare nounstem. `buildNawatLinkedGrammarPathStageGenerateWordRequest()` turns a selected stage into a direct request, `executeNawatLinkedGrammarPathStage()` executes it with linked-path provenance, `previewNawatLinkedGrammarPathNextSource()` previews the supported route-family candidates for that next source without executing it or mutating state, `previewNawatLinkedGrammarPathChain()` composes an explicit user-selected sequence of stages into a chain preview, `buildNawatLinkedGrammarPathSelectionSummary()` exposes the current source plus appendable next route/stage choices without executing stages, selection-state helpers can store/clear/backtrack an explicit selected stage chain without executing it, and `executeNawatLinkedGrammarPathChain()` executes the selected sequence while preserving linked-path provenance. The visible linked-path controls now live dynamically in `#3 SALIDA` as output composition, not as a separate `ruta nawat` rail. Reusable stages are labeled as `Siguiente fuente`, `Salida de etapa`, `Continuaciones`, and generated Andrews source evidence; selection summaries use `Fuente actual`, `Opciones siguientes`, and `Siguiente salida`. The output surface can show appendable next choices plus `seguir` actions that store the chosen next route without executing it. Selected chains render back as `Trayecto` with current-source and next-option labels; `atrás` backtracks the last selected stage, `borrar` clears the chain, `generar trayecto` executes the stored chain on request and renders the final surface metadata; `usar etapa N` promotes any executed stage's generated surface as the new linked-path source while retaining wrapped source-input provenance; `usar salida` promotes the final generated surface as the new linked-path source and syncs that source into the visible input only for the explicit promotion action; the selected-path summary can then offer multiple `seguir` choices from that promoted/current source. Configured profiles are marked with `routeProfileSource: "static-modes"`. Rendering can show family, verbalizer, Andrews contract, pending Andrews contract count, Nawat-only route families, generated `ti`/`hui`/`ya`/`tla`/`o-a`/§55.6 source-evidence labels, source-satisfied continuation chips, traditional-spelling ambiguity labels for `tia`/`huia`, and partial-coverage labels from that metadata.

`generateNawatDenominalAndrewsContractRoutePreview()` now provides the Andrews-backed route-target layer for the full verified inventory: it converts the Classical suffix sequences to Nawat/Pipil rule letters and builds VNC stem plus route-input targets for the supplied source stem, including `hui -> wi`, `hui-lia -> wi-lia`, `ti-ya`, `hui-ya -> wi-ya`, `ya`, `ya` deleted before `lia`, limited `a`, `hua -> wa`, causative and intransitive `tla -> ta`, intransitive `tla` replaced by `ti` before causative `a` and applicative `lia`, causative `tla` replaced by `ti` before applicative `lia`, `o-a -> u-a`, `huia -> wia`, §55.3 note 2 `i-l-huia`/`a-l-huia -> i-l-wia`/`a-l-wia`, and `i-hui`/`a-hui -> i-wi`/`a-wi`. It also records verified Andrews verbstem class contracts where the PDF states them: included-possessor stems and §55.2 causative `tla` as Class A, §54.4 possession `ti` as Class A/B with no deverbal `ya`, §54.2/§54.4 `ti-lia` causatives as Class C once a generated `ti` source is supplied, §54.2 `ti-ya` as Class A/B, §54.2 `hui-ya` as Class B, §54.5/§54.6 and §55.3 `o-a`/`huia` targets as Class C, and §55.6 `i-hui`/`a-hui` source targets as Class B with the `o-a` counterpart as Class C. Source-limited routes now preserve the PDF source requirements before finite routing: §54.3 included-possessor `ti` requires a possessive-state NNC predicate, §54.2.2 `hui-lia` requires a generated intransitive `hui` verbstem source, §54.2.3 `ti-ya`/`hui-ya` require generated `ti`/`hui` intransitive verbstem sources, §54.2.3 `ya-lia` requires a generated intransitive `ya` verbstem source and deletes `ya` before `lia`, §54.2/§54.4 `ti-lia`, §54.5 `ti-a`, and §54.6 `t-ia` require a generated intransitive `ti` verbstem source, §55.1 `tia` requires a temporal compound nounstem, §55.2 `tla -> ti-lia` requires the generated causative `tla` source, §55.2 note `tla -> ti-a/ti-lia` requires the generated intransitive `tla` source, §55.3 note 2 `i-l-huia`/`a-l-huia` requires a generated intransitive `o-a` source and records that Andrews says the route invents a hypothetical `i-hui`/`a-hui` stem while bypassing the transitive `o-a` step, §55.4 `huia` requires an adverbial nounstem, §55.5 `o-a`/`huia` requires a relational compound or possessive relational predicate source, and the §55.6 `o-a` counterpart requires an `i-hui`/`a-hui` source. The §54.5 finite route currently represents only the single-object `ti`-source path; Andrews' possessive-state double-object §54.5 path remains recorded as unmodeled rather than routed through a single-object VNC request. Generated ordinary possessive NNC outputs can now supply bounded §54.3 source evidence from their Nawat surface, so `nukal` can preview and route the included-possessor `ti` target as `(nukalti)` while recording that the possessor remains inside the verbstem and is not transformed into an object. Generated ordinary NNC outputs can also supply the predicate nounstem for §54.4 possession `ti`, so `kal` previews/routes `(kalti)` while keeping the nounstem-focused possession contract separate from the included-possessor path. A selected generated `vi-ti` verbalizer stage, a generated §54.2 inceptive/stative `ti` target, or a generated §54.4 possession `ti` target now supplies bounded `ti` source evidence for the following `ti-ya`, `ti-lia`, `ti-a`, and `t-ia` continuation previews. Generated §54.2.2 `hui` and §54.2.3 `ya` targets supply bounded source evidence for their own `hui-ya`/`hui-lia` and `ya`-deleting `lia` continuations. Generated `ti-ya` and `hui-ya` targets then supply bounded `ya` source evidence for the next `ya-lia` preview, deleting final `ya` before `lia` so the next source stem remains the generated `ti`/`hui` stem (`(pusukti)-(lia)`, `(pusukwi)-(lia)`). Traditional spellings `tia` and `huia` are exposed only as ambiguity labels because Andrews warns that those spellings can be confused with causative `tia` and applicative `huia`. A selected generated `vi-iwi`/`vi-awi` verbalizer stage supplies only §55.6 source evidence, including the original base stem for the counterpart preview; a bare noun source still leaves the `o-a` counterpart source-limited. Andrews target routes that themselves create a later source now carry the same bounded evidence: generated §54.2/§54.4 `ti` can satisfy the following `ti-ya`, `ti-lia`, `ti-a`, and `t-ia` previews, generated §54.2.2 `hui` can satisfy `hui-ya` and `hui-lia`, generated §54.2.3 `ya` can satisfy `ya-lia`, generated §55.2 causative `tla` can satisfy the following `tla -> ti-lia` preview, generated §55.2 intransitive `tla` can satisfy the note's `ti-a` and `ti-lia` previews, generated §55.3 intransitive `o-a` can satisfy the note 2 `i-l-huia` and `a-l-huia` applicative previews, and generated §55.6 `i-hui`/`a-hui` can satisfy the `o-a` counterpart preview, without creating lexical evidence. Explicit source-classification helpers now let callers supply confirmed §55.1 temporal compound nounstem evidence, §55.4 adverbial-nounstem evidence, or §55.5 relational compound/possessive relational predicate evidence, then preview and route the `tia`, `huia`, or `o-a`/`huia` VNC targets through the same finite request contract. These helpers deliberately do not treat generated `locativo-temporal`, configured adverbio rows, or relational boundary frames as automatic evidence. These targets remain visible as route targets, but finite request construction returns null until matching source evidence is supplied. Andrews §55.7 `i-a` routes carry the no-intransitive-counterpart boundary, the usual source-final `k/l` pattern after Nawat orthography, and the PDF notes that (1) a w-final seeming `i-a` target may actually be §55.3 `huia` after /w/ + /w/ contraction, (2) the `i` may belong to the source nounstem, and (3) a seeming `i-a` causative may have an `i-hui` intransitive source without following the §55.6 `o-a` counterpart path. These diagnostics flow through finite request/execution provenance without rejecting the route target or creating lexical evidence. Generated denominal output rows now carry that preview in `denominalFamilyProfile.andrewsContractRoutePreview`, and rendering can show the target count, explicit-request count, object-prefix-required count, class-contract count, source-evidence-required count, warning count, note count, sample VNC inputs, and limited Andrews denominal VNC continuation chips. `buildNawatDenominalAndrewsContractRouteGenerateWordRequest()`, `executeNawatDenominalAndrewsContractRoute()`, `activateNawatDenominalAndrewsContractRouteTarget()`, `previewNawatDenominalAndrewsContractRouteNextSource()`, `previewNawatDenominalAndrewsIncludedPossessorRouteFromOrdinaryNncOutput()`, `previewNawatDenominalAndrewsPossessionTiRouteFromOrdinaryNncOutput()`, `previewNawatDenominalAndrewsAdverbialHuiaRouteFromSource()`, and `previewNawatDenominalAndrewsRelationalCompoundRouteFromSource()` provide the explicit VNC routing contract from an Andrews route target or generated/confirmed source into the existing VNC engine or into the next Andrews source-limited preview only after the caller supplies the required tense/object/source evidence. This preserves the preview boundary: stem targets are not finite VNC generation by themselves, no fixture evidence is created, and `generationAllowed` remains false while `routeTargetGenerated` marks the contract preview itself.

Runtime guard: `src/tests/module_runtime_integrity.test.js` and `npm run check:runtime-manifest` verify one direct-import ESM runtime, unique declared modules, complete installers, and no classic source mirror.

§55.7 source-final status is now route/request/execution metadata only. The `i-a` route records Andrews' majority Classical `[c]`/`/l/` source pattern as Nawat `k/l`, the attested minority `/k/`/`/n/` examples as Nawat `k/n` with `/k/` collapsed into the same Nawat `k`, and the w-final `huia` ambiguity as a confirmation warning; this does not reject finite routing, create lexical evidence, or add fixture surfaces.

§54.2 inceptive/stative target class contracts now travel with the route/request/execution metadata. Andrews' source-final rules are represented after Nawat orthography: `ti` resolves to Class A after consonant-final sources and A/B after vowel-final sources, `hui` resolves to Class A after consonant-final sources and Class B after vowel-final sources, root-plus-`ya` and deverbal `ti-ya` record A/B, deverbal `hui-ya` records Class B, limited `a` records Class C, and `hua -> wa` records Class A without collapsing it into §55.3 `o-a`.

§55.1 temporal `tia` now has the same explicit source-classification boundary as §55.4 and §55.5: `previewNawatDenominalAndrewsTemporalTiaRouteFromSource()` accepts a confirmed compound-temporal NNC source with a time-segment matrix and numeral embed, satisfies the `temporal-compound-nounstem` requirement for finite intransitive `tia` routing, and explicitly does not treat generated `locativo-temporal` rows as automatic evidence.

The modern runtime gate is repository-wide. New lesson work adds or edits canonical `.mjs` modules directly and registers new capabilities once in `RUNTIME_INSTALLERS`; it must not regenerate a `.js` compatibility source.

## Safe Order Template

1. Define the grammar/UI contract for one small target.
2. Add focused failing tests.
3. Patch engine/data/state/UI only where needed.
4. Verify default behavior did not change outside the target.
5. Run validation and test suites.

## Current Review Buckets

The current checkpoint should be reviewed as separate buckets, not as one broad diff:

1. Category-first curriculum/docs contract: `AGENTS.md`, `docs/CODEX_BOARD.md`, `docs/GRAMMAR_SPEC.md`, and this file.
2. Curriculum index UI: `index.html`, `style.css`, `src/ui/curriculum/curriculum.mjs`, and curriculum-focused tests.
3. Ordinary NNC curriculum references: `src/core/nnc/nnc.mjs` and the related NNC tests.
4. Lessons 35-41 nominal/adjectival metadata: canonical `.mjs` modules under `src/core/`, `src/ui/rendering/rendering.mjs`, focused grammar/UI tests, and `src/tests/module_runtime_integrity.test.js`.
5. Lessons 44-50 adverbio/relational/place/adjunction-boundary metadata: canonical `.mjs` generation/rendering modules and their focused tests.

Do not start new lesson fixtures until these buckets are committed or deliberately held.

## Standard Verification

- `npm run check:data`
- `npm test`
- `npm run test:regression`
- `npm run verify:readiness`

## Completed Decisions

- `nounClass` is restricted to `t`, `ti`, `in`, and `zero`.
- Former pseudo-classes `lexical` and `open` are represented through source metadata instead.
- Open-stem ordinary NNC generation remains dynamic and opt-in, defaulting to class `zero` unless the user selects `t`, `ti`, or `in`.
- UI now labels the S board as `NNC/S`, class as predicate class, and animate plural as `Plural` rather than generic `Tipo`.
- `formulaSlots` now backs the visible ordinary NNC formula echo.
- `Fórmula CNN: #...#` is a rendered echo, but the shared Andrews formula schema now drives the formula-authority gate through `evaluateAndrewsFormulaGenerationAuthority()`.
- `categoryProfile` now derives predicate state, possessive state, animacy, and reference labels from existing result fields and `formulaSlots`.
- Output row category labels are explicit, but unsupported possessive generation still displays diagnostics instead of inferred forms.
- Ordinary NNC UI gating uses side-effect-free render probes only for control availability; probes are not generation truth.
- `Estado/poseedor` disables unavailable possessor prefixes, keeps `Ø` enabled, and preserves diagnostics for selected unsupported possessive requests.
- Composer class control is now `Conector num1-num2`; fixture-fixed class/animacy lock conflicting options.
- Animate plural is labeled as `Referencia plural`, and inanimate reference remains common/distributive.
- Class/stem compatibility is now enforced: `(...V)t`, `(...C)ti`, `(...C)in`, and `(...C/V)Ø`.
- `ti` and `in` remain open-stem structural only until evidence-backed Nawat/Pipil fixture forms are confirmed.

## Current Milestone

Milestone: Ordinary NNC Foundation v1 - Lessons 12-14.

Phase 1-7B establishes the working ordinary NNC engine/UI contract for Lessons 12-14:

- Lesson 12 is mostly solved as an engine/UI foundation through `formulaSlots` and explicit connector placement.
- Lesson 13 is partially solved through possessive-state metadata, UI gating, and diagnostics for unsupported possessive requests.
- Lesson 14 is structurally corrected through the restricted `nounClass` set and class/stem compatibility.

The foundation is structurally stable, but not data-complete. Do not add `ti` or `in` fixtures without evidence-backed Nawat/Pipil forms, and do not treat open-stem structural examples as fixture evidence.

## Remaining Small Targets

- Add browser smoke coverage for the S board once a dev server target is established.
- Lesson 1 concept and notation metadata now has a visible glossary UI derived from `core/concepts`; keep it diagnostic-only and non-generative.
- For Lesson 3, keep `core/particles` and the visible Partícula mode diagnostic-only. The current seed list is transferred from Andrews Lesson 3 through the Nawat/Pipil orthography bridge; local particle inventory data may confirm fixture status, but Andrews particle syntax must decide any future generation contract. Parenthesized stem/CN syntax must remain out of scope for Partícula, and seed-preview rows must keep separate `inventory-preview` provenance.
- For Lessons 1-4, keep glossary, particles, and clause formulas separate from current word-generation engines until explicit metadata/data targets exist.
- For Lesson 2, keep modern Nawat phonology separate from the diagnostic-only orthography bridge; bridge metadata/status messages must not generate Nawat/Pipil forms.
- For Appendix F, treat the existing orthography bridge as partial diagnostic support only; do not add Appendix F spelling normalization without confirmed alias data and tests.
- For Lesson 4, keep `core/clause` nuclear-clause shell metadata diagnostic-only; the calculator summary and generated output row labels may display CNV/CNN Lesson 1 terminology while compatibility `formulaType` remains `VNC/NNC`, but shell metadata must not drive generation or sentence syntax.
- For Lessons 5-7, treat the core CNV/VNC motors as implemented; diagnostic formula-slot metadata, `Fórmula CNV` rendering, `vncValencyFrame` subject/object/valency labels, and preterit `verbstemClassProfile` labels may explain subject/object/predicate/tense order, Lesson 5-6 valency frames, and Lesson 7 class selection, but future work should stay explanation/UI unless tests reveal a concrete engine gap.
- For Lessons 8-10, keep `core/sentence` diagnostic-only and opt-in; sentence-level negation, question, emphasis, optative, and admonitive behavior must stay out of finite VNC generation until an Andrews sentence source model and orthography bridge exist.
- For Lesson 11, add irregular behavior from Andrews irregular contracts plus focused tests; Nawat/Pipil evidence may confirm lexeme spelling, not decide the grammar.
- Expand evidence-backed possessive/allomorphy examples only when the user supplies Nawat/Pipil forms or repo data clearly supports them.
- Continue evidence hunt for fixture-backed `ti` and `in`.
- For Lesson 15, read Andrews natural/required possession rules before proposing schema fields such as possession type or state-case availability; Nawat/Pipil examples can confirm fixture spellings after the state contract exists.
- Do not add Lesson 15 possession metadata, diagnostics, generation, or UI labels until the Andrews state contract and validation tests are planned.
- For Lesson 16, build pronominal NNC behavior only from Andrews pronominal-NNC contracts plus the orthography bridge; confirmed Nawat/Pipil examples can confirm fixture spellings afterward.
- Do not treat pronoun/agreement labels, ordinary person labels, generated/open-stem examples, or Andrews/Classical forms as pronominal NNC fixture evidence.
- For Lessons 17-19, derive supplementation/topic schema, parser/search metadata, diagnostics, tests, and UI representation from Andrews clause contracts; Nawat/Pipil clause examples can confirm visible fixture spellings afterward.
- Do not treat ordinary NNC word generation, VNC rows, pronoun/agreement labels, parser compound examples, UI topic/focus wording, translation labels, or Andrews/Classical examples as supplementation evidence.
- Lessons 20-26 are implemented motors and no longer count as pending faltantes; any future 20-26 work should be explanatory/status/UI refinement unless a test proves a concrete engine gap. Diagnostic `derivedVoiceFrame` metadata/rendering may explain nonactive/passive/impersonal voice and source-to-target valency, and diagnostic `forwardDerivationFrame` metadata/rendering may explain causative/applicative stem and valency paths, but both must remain display-only.
- Keep Lesson 27 frequentative boundary metadata isolated from generic reduplication helpers and distributive rendering.
- Do not add Lesson 27 frequentative data, generation, or UI until the Andrews frequentative source contract, orthography bridge, and focused tests are planned.
- For Lessons 28 and 30, treat current `compoundAst` and parser/composer support as partial parser metadata, not a full compound generator.
- Diagnostic `compoundFrame` metadata/rendering may explain matrix/embed structure for accepted compound VNC inputs, but it must remain derived from `compoundAst` and display-only.
- Lesson 29 `core/vnc/purposive` now has a narrow Andrews-logic generator for outbound/inbound purposive target stems; keep passive/impersonal embeds, compound-stemmed embeds, external directionals, and full finite UI expansion scoped until their Andrews contracts are separately tested.
- Keep Lessons 31-32 `core/nnc/compound` as non-generative compound/affective boundary metadata; do not turn VNC `compoundAst`, ordinary NNC fixtures, or parser punctuation into compound-NNC fixtures.
- Lesson 33 `core/vnc/honorific_pejorative` now has a narrow Andrews preterit-embed target-stem generator for `tzin-o-a` honorific and `pol-o-a` pejorative matrices; keep route choice, reverential doubling, compound-verbstem targets, and full finite UI expansion scoped until separately tested.
- Keep Lesson 34/Appendix D `core/nnc/numerals` scoped: basic simple-count cardinal stems one through four may generate from Andrews slots through the orthography bridge, while gross counts, high-order compounds, conjoined numerals, classifier sets, reduplication, approximation, and measures remain diagnostic until separately tested.
- Do not treat parser slash/dash syntax or ordinary NNC lexical-embed classifications as compound-NNC, affective, purposive, honorific, or numeral fixture evidence.
- For Lessons 35-41, treat current `sustantivo` and `adjetivo` motors as partial coverage with `nominalizationProfile` metadata; new generation requires Andrews source contracts, the orthography bridge, and focused tests.
- `core/nnc/nominalization` is a boundary/status module. Current `sustantivo-verbal` generation covers the active-action `s/lis` motor from Andrews `z/liz` through `getActiveActionNominalizerContract()`, including root-plus-`ya` alternates that keep `s/lis` instead of falling through to bare VNC stems; current `agentivo-preterito` output can feed Andrews 35.9-35.10 ownerhood/abundant-ownerhood VNC matrices from generated `#3 salida` stems, and ordinary NNC output can feed the first class-compatible ownerhood matrices (`t` -> `e/ej`, `zero/in` -> `wa/waj`, abundant `yua/yuj`), while complete `z/liz` fixture coverage and full patientive-family candidates remain unconfirmed.
- `core/nnc/adjectival` is a narrow opt-in generator plus boundary module. It can generate the current Andrews 40.1/40.9 adjectival-NNC surfaces, re-route Andrews 40.3 generated VNC surfaces as adjectival function while keeping their VNC shell, classify Andrews 40.10/40.11 source patterns, intensify generated adjectival NNC formula slots under Andrews 41.1, and let Andrews 41.2 compound-source `adjetivo` outputs expose compound source roles from `#3 salida` without changing Nawat surfaces; it must not generate modifier/head ASTs or sibling synonym forms without Andrews route contracts and orthography-bridge coverage.
- `patientiveFamilyProfile` is a partial Lessons 37.9-39 output-stage taxonomy for existing patientivo sources only; it is not fixture evidence. Explicit `passive` and `impersonal` source requests now remain separate grammar-source categories in routing and generation: passive rejects intransitive ultimate sources, clears single-object nonspecific `ta`/`te`, maps source reflexive `mu` to shuntline `ne`, and keeps exactly one selected projective (`ta` or `te`) from double-projective sources and permits the deleted-`te` alternate when the selected projective is `te`; impersonal allows intransitive sources, keeps `ta`, maps single-object mainline `te` to the impersonal `ta` pattern, maps source reflexive `mu` to shuntline `ne`, and preserves double-projective `te+ta` because shuntline `ta` already marks the nonhuman patient; perfective/imperfective patientive branches use the same single-object `te` -> `ta` rule, map source reflexive `mu` to shuntline `ne`, and preserve `te+ta`; perfective patientive generation is gated by a structured Andrews 39.1 source-ending contract that converts Classical rule spellings to Nawat letters without creating fixture evidence, imperfective patientive generation now uses a structured Andrews 39.2 source-stem contract from the VNC core rather than a finite predicate row, so tense material such as `ya` cannot leak into the patientive NNC predicate; characteristic-property `calificativo-instrumentivo` output keeps Nawat `-yu` in possessive state (`mikkayut` -> `numikkayu`) instead of dropping the matrix, characteristic-property embed continuation now omits possessive `-yu` and promotes mapped possessors to outside object prefixes (`numikkayu` -> `nechmikkachikawa`), with `formationFrame` distinguishing the omitted `(-yo)-tl-`/`-yu(t)` matrix from the incorporated embed root; root/stock patientive noun output is restricted to tli-class connector surfaces and rejects explicit `in` requests while explicit route-stem requests stay available, dynamic patientive V->S routes ask `#3 salida` for the generated noun surface instead of statically rebuilding suffix outputs, row rendering no longer direct-repairs patientive output by route-surface or suffix replacement, the prelocative continuation also reads the generated row surface before building its incorporated-root VNC action, 39.6 compound continuations now expose `formationFrame` metadata for generated patientive embed, data-backed matrix, and output input without creating new fixture evidence, `formationFrame` now distinguishes Andrews 39.7 object-complement continuations from Andrews 39.8 incorporated-object continuations, compound-source patientive outputs now preserve the parsed compound source in `patientiveCompoundSourceFrame` so identical surfaces can still retain passive/impersonal source-family distinctions, the current pre-locative/incorporated-root destination promotes a selected possessive patientive possessor into the existing object slot when `POSSESSIVE_TO_OBJECT_PREFIX` has a mapping, unmapped/mixed possessor choices remain blocked instead of guessed, the shared nonactive builder now enforces Andrews' tli-class patientive connector contract for `lu/luwa/u/uwa/walu` source families instead of generating zero/`in` spillover, the nonactive patientive stem builder now derives deletion/retention from the same source-suffix contract instead of a separate hardcoded suffix table, and the selected `#3 salida` row carries `patientiveSourceStageFrame` with its nonactive suffix, perfective source-ending, or imperfective source-stem contract plus Andrews operation and Nawat output prefix/stem/connector.
- Andrews 39.4 root/stock patientivo now has the same contract shape as the nonactive, perfective, and imperfective branches: `patientive-root-stock-source-contract` records root-or-stock source, tli-class target, Nawat `t/ti` connector, and `c/x/z/ch` -> `k/sh/s/ch` orthography conversions while keeping variant selection diagnostic and generated surfaces unchanged.
- Andrews 39.5 multiple patientive derivation now has a generated-output contract: `patientive-multiple-derivation-contract` probes the current patientivo input across implemented source procedures, records the available procedure inventory, and keeps synonymy/idiomatic translation value diagnostic instead of merging or inventing surfaces.
- Andrews 36.2-36.3 `agentivo` is treated as a customary-present agentive NNC route: the generated `ni` remains visible in the Nawat surface but belongs to the predicate stem, while the subject-number connector is exposed as `Ø`, `met`, or `wan`. The fully nominalized §36.3 output can now continue dynamically from `#3 salida` into the first data-backed nominal compound matrix (`kal`) through ordinary NNC generation and into the first data-backed verbal matrix (`tuka`) through a real transitive VNC compound action (`nemini` -> `-(nemini/tuka)` -> `kineminituka` with object `ki`).
- Andrews 36.7 `agentivo-presente` is treated as a separate opt-in present-agentive NNC route: the generated present predicate is the nounstem, the route remains absolutive-only, and the present source number connector remains visible as `num1-num2` instead of being collapsed into the customary-present `agentivo` route.
- Andrews 35.3/35.5-35.12 `agentivo-preterito` is treated as a separate opt-in preterit-agentive NNC route: restricted-use absolutive output keeps the generated preterit surface but represents the predicate stem and preterit connector separately in `#3 salida`; possessive-state probes switch to the general-use Nawat `-ka` matrix with `w/wan` connectors and shuntline `ne` for reflexive sources. The first 35.7 continuation now lives in generated output rows: the row derives the general-use `-ka` stem and can promote it into real compound VNC/NNC inputs with Nawat data-backed matrices, instead of showing a separate output rail. The same generated stem can now feed Andrews 35.9-35.10 ownerhood matrices as real VNC inputs with Nawat orthography (`-wa` -> `-wajka`, `-yua` -> `-yujka`), Andrews 35.12 incorporated-complement VNC matrices with an outside Nawat object slot (`(tamatka/mati)` -> `kitamatkamati`, `(tamatka/talia)` -> `kitamatkatalia`), and the first Andrews 35.12 adverbial-manner VNC matrix (`(tamatka/nemi)` -> `tamatkanemi`). Ordinary NNC output rows can also promote their generated nounstem into first ownerhood/abundant-ownerhood VNC inputs through class-compatible matrix routing (`shuchit` -> `(shuchi)-(e)` -> `shuchiejka`, `kal` -> `(kal)-(wa)` -> `kalwajka`, abundant `shuchiyujka`), with `ti` ownerhood left diagnostic until confirmed subclass evidence exists.
- Andrews 36.8 `agentivo-futuro` is treated as a separate opt-in future-agentive NNC route: restricted-use output keeps future `s` inside the predicate stem with `ki/ket` as the NNC connector, while possessive-state probes use the general-use Nawat `-ka` matrix with `w/wan` connectors.
- Andrews 36.5 `potencial-habitual` is treated as a customary-present patientive NNC route: single projective `ta/te` sources do not keep a projective object, double-projective sources keep exactly the selected projective (`ta+te` -> `tamachuni/tamatuni/tamatiluni`; `te+ta` -> `temachuni/tematuni/tematiluni`), source reflexive `mu` maps to shuntline `ne`, and possessive-state probes remain absolutive.
- Andrews 36.10-36.11 `calificativo-instrumentivo` general-use action nouns now treat the source VNC subject as the source of the Nawat possessor when no possessor is supplied (`ni` -> `nu`, `ti` -> `mu`, `Ø` -> `i`, `ti...t` -> `tu`, `an...t` -> `anmu`, `Ø...t` -> `in`). That exception is limited to explicit `actionNounStemUse: "general-use"` output; restricted characteristic-property output still requires the NNC subject to remain nonanimate.
- Andrews 36.12 is now locked as a source-of-possessor contract in `nominalizationProfile.possessorSourceFrame`: active/passive action general-use nouns mark the possessor as transformed from the source VNC subject, while possessive preterit-agentive nouns mark the possessor as external and keep the source/NNC subject in the subject slot.
- Andrews 36.6 `instrumentivo` is treated as a two-source NNC route: absolutive output uses the customary-present impersonal/nonactive source, possessive output uses the imperfect active source, explicit possessive-mode requests can transform the source VNC subject into a Nawat possessor (`ni` -> `nu`, `ti` -> `mu`, `Ø` -> `i`, `ti...t` -> `tu`, `an...t` -> `anmu`, `Ø...t` -> `in`), generated `#3 salida` rows can show those possessive continuations dynamically, source reflexive `mu` maps to shuntline `ne`, and `ni/ya` source-tense material remains inside the predicate stem rather than the subject-number connector.
- Andrews 40.10-40.11 `adjetivo-preterito` source patterns are now engine-classified, not generated as sibling synonyms: current root-plus-`ya` adjectival output records `k-ti-ya/c-ti-ya` sources as synonymous-pair patterns and `z-ti-ya` sources as synonymous-triplet patterns in `sourceFormationFrame`, while preserving Nawat/Pipil spelling and generating only the requested source surface.
- Do not treat current generated nominal/adjectival surfaces as complete evidence for ownerhood, complete `z/liz` fixture coverage, full patientive-family taxonomy, or adjectival modification.
- Keep Lessons 42-43 `core/clause/modification` as a non-generative adjectival-modification AST contract: it may compose supplied generated head/modifier outputs and record order/marker/scope/link metadata, but it must remain separate from ordinary NNC `formulaSlots`, parser/search detection, UI actions, and new word generation until Andrews modifier/source contracts and orthography-bridge coverage justify more.
- Keep Lesson 44 `core/clause/adverbial` as a non-generative adverbial nuclear-clause frame contract; `pasado-remoto-adverbio-activo` remains partial configured adverbio surface support only.
- `adverbialNuclearClauseFrame` metadata/rendering may explain the source VNC, source valency, first-degree subject-pronoun adverbialization, and manner-surface scope for generated configured adverbio rows, but it must remain tied to `#3 salida` output and must not generate new adverbial forms.
- Keep the configured adverbio output outside the linked output-composition surface unless a future target explicitly redesigns route semantics.
- Keep Lessons 45-47 `core/nnc/relational` as non-generative relational-NNC usage-frame/boundary metadata; do not treat locative-temporal nominal outputs, route labels, static labels, or place/preposition translations as relational NNC fixture evidence.
- Current relational usage frames may record Andrews 45.2-46 structure (option-one possessive-state/simple-stem limits, option-two integrated matrix, option-three linked matrix, option-four embedded position, and impersonal option-two absolutive-state diagnostics), but they must not generate or alter Nawat/Pipil surfaces.
- Diagnostic `relationalNncBoundaryFrame` metadata/rendering may mark generated `locativo-temporal` nominal rows as unconfirmed relational candidates, but it must remain a false-positive/evidence-boundary label and must not generate relational forms.
- Keep Lesson 48 `core/nnc/place_gentilic` as non-generative place-name/gentilic NNC usage-frame/boundary metadata; do not treat ordinary NNC fixtures, relational metadata, locative-temporal nominal outputs, route labels, CSV verb rows, calendar roadmap text, or place/profession/gentilic translations as place/gentilic fixture evidence.
- Current place/gentilic usage frames may record Andrews 48 structure (unique socially designated place-reference vs context-chosen locative relation, ordinary/adverbial/adjectival place-name uses, topographical-feature boundary, gentilic two-clause structures, `-ca` matrix derivations, pan-e-ca vs pan-ca distinction, `yo` collectivity, and profession extensions), but they must not generate or alter Nawat/Pipil surfaces.
- Diagnostic `placeGentilicNncBoundaryFrame` metadata/rendering may mark generated `locativo-temporal` nominal rows as unconfirmed place/gentilic candidates, but it must remain a false-positive/evidence-boundary label and must not generate place-name or gentilic forms.
- Keep relational NNC fixture data/generation pending until Andrews relational contracts and the orthography bridge justify `data/static_relational_nnc` or engine behavior.
- Keep place/gentilic fixture data/generation pending until Andrews place/gentilic contracts and the orthography bridge justify `data/static_places`, `data/static_gentilics`, or engine behavior.
- Keep Appendix E `core/calendar` as non-generative calendar-name boundary metadata; do not add calendar-name fixtures or generation until Andrews calendar contracts and the orthography bridge justify `data/static_calendar` or engine behavior.
- Keep Lessons 49-50 `core/clause/adjunction` as non-generative adverbial-adjunction AST/boundary metadata; do not treat single generated words, translations, route labels, particle labels, CSV verb rows, or existing adverbial/relational/place boundary metadata as clause-adjunction evidence.
- Current adverbial-adjunction ASTs may compose supplied clause/unit surfaces and record Andrews 49-50 structure (modifier/head order, recursion, place/time apposition, nonadverbialized clause-unit relations, condition marking, purpose/concession/consequence/proviso markers, and `ca` reason as non-conjunction), but they must not generate or alter Nawat/Pipil surfaces.
- Keep adverbial-adjunction static data/generation work pending until Andrews adjoined-unit or clause contracts plus the orthography bridge justify `data/static_adverbial_adjunction`, parser/search detection, generation, or UI.
- For Lessons 54-55, treat current denominal routes and source-state metadata as partial support, not a complete denominal-family engine. Keep `vt-na` explicitly Nawat-route-only until an Andrews suffix-family contract is confirmed; Nawat/Pipil evidence alone cannot justify it.
- Keep Lessons 51-53 `core/clause/complement`, `core/clause/conjunction`, and `core/comparison` as non-generative boundary metadata; do not treat VNC/NNC outputs, object controls, subject labels, nominalization profiles, parser separators, slash/CSV variants, particle labels, adjective-like outputs, degree/question labels, translations, route labels, or single generated words as confirmed complement/conjunction/comparison evidence.
- Keep complement/conjunction/comparison data/AST work pending until Andrews clause contracts plus the orthography bridge justify `data/static_complements`, `data/static_conjunctions`, `data/static_comparisons`, schema, generation, or UI.
- Keep Lesson 51 `core/clause/complement` as non-generative complement AST/boundary metadata; do not treat VNC/NNC outputs, object controls, subject labels, nominalization profiles, route labels, translations, or single generated words as confirmed complement evidence. Current complement ASTs may compose supplied surfaces and record Andrews 51 object/subject/adverbial complement links, passive object-complement transforms, and relational adverbial-complement incorporation risk, but they must not generate or alter Nawat/Pipil surfaces.
- Keep Lesson 52 `core/clause/conjunction` as non-generative conjunction AST/boundary metadata; do not treat parser separators, slash/CSV variants, particle labels, translations, route labels, or single generated words as confirmed conjunction evidence. Current conjunction ASTs may compose supplied surfaces and record Andrews 52 balanced no-head coordination, marked `auh`, unmarked juxtaposition, adverbial modifiers that are not conjunctors, correlative particles, lexical innovation, and parallel structure, but they must not generate or alter Nawat/Pipil surfaces.
- Keep Lesson 53 `core/comparison` as non-generative boundary metadata until Andrews comparison contracts and the orthography bridge justify comparison ASTs, data, generation, or UI.
- Keep Lesson 56 `core/nnc/names` as non-generative personal-name NNC boundary metadata; do not treat ordinary NNC fixtures/open stems, capitalization labels, proper-name translations, place/gentilic metadata, adjunction/conjunction metadata, or calendar roadmap text as confirmed personal-name evidence.
- Keep Lessons 57-58 `core/analysis` as non-generative textual-analysis/miscellany boundary metadata; do not treat VNC/NNC outputs, sentence-layer metadata, clause-shell metadata, parser labels, topic/focus UI labels, bare `mah` strings, translations, or CSV verb rows as confirmed analysis evidence.
- Keep personal-name and textual-diagnostic data/AST work pending until Andrews name/textual-analysis contracts plus the orthography bridge justify `data/static_names`, `data/static_analysis`, schema, generation, or UI.
- Consider a later UI distinction between fixture-backed evidence and dynamic preview output, without changing generation semantics.
- Consider adding a visual slot breakdown if the single formula echo is not enough for teaching, but keep it derived from `formulaSlots`.
- Consider richer visual treatment for locked fixture metadata if disabled controls are not clear enough in browser review.
