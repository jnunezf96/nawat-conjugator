# Current Target

## Repository coordination cleanup and readiness handoff

Date: 2026-07-19

Status: Active. The comparison migrations are implemented; lifecycle cleanup and the complete resumable readiness gate are the current explicit target.

### Explicit implementation target

1. Close finished shared actions only when every original responsibility is aligned, temporary support is retired, recorded retirements are complete, and the obsolete deciding path is explicitly removed.
2. Preserve the accumulated target history outside this live coordination file so `CURRENT_TARGET.md` remains bounded and actionable.
3. Keep the six consolidated protocol vocabularies authoritative through their typed owners: tense/mood, derivation type, voice, source transitivity, ordinary NNC noun class, and output scope.
4. Preserve intentional contextual differences in sentence modes, Lesson 9 particles/modifiers, person inventories, directionals, and NNC/VNC paradigm dimensions.
5. Run `npm run audit:alignment` after lifecycle and history cleanup.
6. Run the complete resumable `npm run verify:readiness` gate through browser delivery. Focused evidence does not substitute for this handoff gate.

### Shared action

The machine-readable assignment is `docs/shared_actions/active/repository-readiness-coordination-cleanup.json`.

### Participating responsibilities

- The original grammar, application, UI, runtime, verification, and coordination actors retain responsibility for their completed slices; this cleanup does not transfer or dilute them.
- The stabilization manager reconciles only shared-action lifecycle and historical-board state.
- Readiness verification evaluates the integrated worktree without creating a second runtime or authority path.

### Completion boundary

This target closes only when:

- no finished shared action remains falsely active or lacks its required old-authority-removal witness;
- the live target is within the alignment bound and the full historical record remains available;
- `npm run audit:alignment` reports `ALIGNED`;
- `npm run verify:readiness` passes every exact-input responsibility, including fresh alignment and browser delivery;
- no obsolete authority path or temporary compatibility adapter remains.

### Preserved history

The preceding detailed targets and completion evidence are retained in `docs/CURRENT_TARGET_HISTORY_2026-07-17_TO_2026-07-19.md` and the chronological `docs/CODEX_BOARD.md`.
